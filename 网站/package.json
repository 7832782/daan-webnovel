{
  "name": "daan-webnovel-site",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node sync.mjs",
    "dev": "node sync.mjs && vitepress dev src",
    "build": "node sync.mjs && vitepress build src",
    "preview": "vitepress preview src"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}